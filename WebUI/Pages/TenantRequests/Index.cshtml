@page
@model RentalRepairs.WebUI.Pages.TenantRequests.IndexModel
@{
    // This page exists solely to redirect /TenantRequests to /TenantRequests/List
    // No content needed as the redirect happens in OnGet()
}